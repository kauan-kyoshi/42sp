/*
Assignment name  : lcm
Expected files   : lcm.c
Allowed functions:
--------------------------------------------------------------------------------

"Escreva uma função que recebe dois unsigned int como parâmetros e retorna o MMC
(Mínimo Múltiplo Comum) calculado desses parâmetros.

O MMC (Mínimo Múltiplo Comum) de dois números inteiros não-nulos é o menor 
inteiro positivo que é divisível por ambos os inteiros.

O MMC pode ser calculado de duas maneiras:

- Você pode calcular os múltiplos de cada inteiro até encontrar um múltiplo 
comum diferente de 0.

- Você pode usar o MDC (Máximo Divisor Comum) desses dois inteiros e calcular 
da seguinte forma:

MMC(x,y)=∣x∗y∣/MDC(x,y)

∣x∗y∣ significa "Valor absoluto do produto de x por y"

Se pelo menos um dos inteiros for nulo, o MMC é igual a 0.

Sua função deve ter o seguinte protótipo:

  unsigned int    lcm(unsigned int a, unsigned int b);
*/

unsigned int    lcm(unsigned int a, unsigned int b)
{
	
}
